from library.dataICC import GeoEnum

from enum import Enum

class Color(Enum):
    RED = 'red'
    GREEN = 'green'
    BLUE = 'blue'

data = True

for i in GeoEnum if data else list(GeoEnum)[list(GeoEnum).index(GeoEnum.BENGKULU):]:
    print(i)


print()